{{>header}}
{{>navbar}}
<section class="container my-3">
    <div class="border border-3 border-primary p-4">
        {{#if isNotFound}}
            <div class="alert alert-danger">
                user not found
            </div>        
        {{else}}
        <h5> Name: {{user.name}}</h5>
        <p> address: {{user.address}}</p>
        <p>
            phone: {{user.phone}}
        </p>
        <p>
            accNum: {{user.accNum}}
        </p>
        <p>  intialbalance: {{user.intialbalance}}   </p>
        
        {{/if}}
    </div>
</section>
{{>footer}}